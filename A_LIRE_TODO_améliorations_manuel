Ce qui ne marche pas / qui reste à faire / qui est améliorable :
    - Barres de progression
    - Protein Domains :
        - réfléchir à un output graphique
        - Je ne sais pas si l'output s'actualise pas quand on change les paramètres (problème de reactive values)
        - les p values sont presque toutes les mêmes (en page 1 en tout ca,s oublié de voir le reste ...)
            En fait je crois que notre jeu de données a déjà filtré les trucs pas significatifs
    - KEGG : ça marche, mais c'est un petit bricolage, qui ne marche que sous windows. 
        Une astuce de Maxime : convertir l'image d'output en plot, et utiliser renderPlot
    
A vérifier : 
    - que les 3 analyses ne buguent pas entre elles : il faut qu'elles soient éxéctuables dans n'importe quel ordre (j'ai pas vérifié, pour l'instant j'ai lancé 
    que dans l'ordre GO, puis KEGG, puis Protein Domains. J'ajoute des print un peu partout et je teste ça.
    - Par contre, il ne faut pas les lancer en simultané
    
Consigne d'utilisation : 
    - il faut créer un dossier "www" dans le même répertoir que le script shiny :
        -> c'est là que sont stockées les images de pathview et c'est que Shiny va par défaut les chercher.
    - Configuré pour le fichier "human_retine.txt" avec les colonnes "Ensembl_Gene_ID" "Gene_Symbol" "log2FC" "p_value"	"adj_pvalue"